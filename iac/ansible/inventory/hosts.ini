# Inventory for ${{ values.client_name }}
# AWS Region: ${{ values.aws_region }}
# Managed by Backstage — do not edit manually

[all:vars]
ansible_user=ec2-user
ansible_ssh_private_key_file=~/.ssh/id_rsa
client_name=${{ values.client_name }}
aws_region=${{ values.aws_region }}

[web]
# Add your EC2 instance IPs here after provisioning
# e.g. 1.2.3.4 ansible_host=1.2.3.4

[db]
# Add your RDS endpoint here after provisioning
